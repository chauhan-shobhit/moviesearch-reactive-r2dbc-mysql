package com.shobhit.r2dbc.moviesearch.configuration;
/*
 * import org.springframework.context.annotation.Configuration;
 * 
 * import io.r2dbc.spi.ConnectionFactory; import reactor.core.publisher.Mono;
 * 
 * @Configuration public class DBConnectionConfiguration {
 * 
 * MySqlConnectionConfiguration configuration =
 * MySqlConnectionConfiguration.builder().host("127.0.0.1").user("root")
 * .port(3306).password("rootuser").database("r2dbc").serverZoneId("UTC").build(
 * );
 * 
 * ConnectionFactory connectionFactory =
 * MySqlConnectionFactory.from(configuration);
 * 
 * Mono<Connection> connectionMono = Mono.from(connectionFactory.create());
 * 
 * }
 */